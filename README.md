# The Game Of Islands in Elixir

## First Part
We will model the domain data with Elixir data types instead of a DB and then we will design functions to transform that data, creating the behavior we want.
